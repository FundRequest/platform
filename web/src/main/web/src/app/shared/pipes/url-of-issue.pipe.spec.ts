import { UrlOfIssuePipe } from './url-of-issue.pipe';

describe('UrlOfIssuePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlOfIssuePipe();
    expect(pipe).toBeTruthy();
  });
});
